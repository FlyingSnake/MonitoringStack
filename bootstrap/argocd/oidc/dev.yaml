configs:
  cm:
    oidc.config: |
      name: Keycloak
      issuer: https://keycloak.dev.example.internal/realms/monitoring
      clientID: argocd
      clientSecret: $oidc.keycloak.clientSecret
      requestedScopes: ["openid", "profile", "email", "groups"]
